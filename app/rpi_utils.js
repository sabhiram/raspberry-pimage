var
    // Node Modules
    _     = require("underscore")._,
    async = require("async"),
    util  = require("util"),
    path  = require("path");

module.exports = function(log_settings) {

    var log = require("./logger")(log_settings);

    return {

    };

}
